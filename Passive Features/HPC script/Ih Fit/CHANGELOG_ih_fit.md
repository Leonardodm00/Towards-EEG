# Ih Fit -- stage log

| Date | Stage | Change |
|---|---|---|
| 2026-09-22 | 0 | Folder created as a byte-copy of `Biological Fit/` (monolith `cmp`-identical). `regression_passive_identity.py` added: run-B configuration (dt 0.1 ms, 60 ms window, 2 smallest steps, 12 mV cap), same seed, loss at 20 random log-theta points and `gp_minimize` output compared between the two folders. PASS in the assistant's sandbox on a synthetic archive cell (max dL = 0, x and fun identical). Cluster check (Step-0 shell block of the v1 handoff) and the run-B config record are still to be done by the user. |
| 2026-09-22 | 1 | `mod/Ih.mod`, `mod/Ih_human.mod` with RANGE `vshift`, `vshift_minf`, `tau_scale`; `human_ih_params.py` extended (section 5); `ih_mechanism.py`; `smoke_ih_fit.py` S1-S5 + S10. All 6 checks PASS with NEURON 9.0.2 (sandbox). Sign convention fixed and pinned by S5: Kalmbach = `vshift = +20`. Axon stub carries no I_h by default (`IhSpec.regions`). |
| 2026-09-22 | 2 | **Loader.** `CellData` gains `long_square_depolarising` (spike-free depolarising Long Square bundles, the I_h validation set) and `sag_ratio` (which `passive_long_step_training`'s sag-gated guard has been reading via `getattr` and never finding). `load_cell_from_archive` gains `ls_max_amplitude_pA=None` (no current ceiling: every recorded hyperpolarising amplitude, D-007), `load_depolarising_ls`, `ls_dep_max_amplitude_pA`, the two spike thresholds and `swc_dir`. New helpers `sweep_has_spike`, `bundle_trough_mV`, `_ls_bundles_from_sweep_info` (the per-amplitude averaging, extracted so both polarities share one implementation). **Every default is the legacy one**, so `load_cell_from_archive(d)` returns exactly what it returned before; the I_h behaviour is opt-in at the call site. |
| 2026-09-22 | 3 | **Parameter vector.** New `param_spec.py`: `ParamAxis` / `ParamSpec` (ordered axes, physical boxes, per-axis log-or-linear coordinate, `as_skopt_dimensions`, `to_physical` / `to_q`, `rail_flags`, `apply`), `PASSIVE_3D`, `make_ih_spec` (4/5/6-D depending on which kinetic knobs are free), `flatten_result_params`. Loss calling convention is now **variadic positional in spec order**, `loss(*q)`, which for the 3-D spec IS the legacy `loss(cm_log, rm_log, ra_log)` -- so `_refit_from_bundles`, `cm_profile_sweep` and every other call site keep working unchanged. `fit_one_cell`, `_build_loss_function`, `_gp_parameter_uncertainty` and `_estimate_residual_noise_at_mle` take a `spec`; `PassiveFitResult` gains `params`, `sigmas_by_name`, `param_spec`, `ih_summary`; `OptimiserInputs` gains `param_spec`. |
| 2026-09-22 | 4 | **Protocol and windows.** `passive_long_step_training.py` gains `ls_rmsd_window_s` (`after_onset` = legacy truncation, `step` = D-006 default, `sweep` = incl. rebound), `ls_subwindows_s` (charge / sag / rebound diagnostics), `assign_ls_roles` and `split_train_validation_ih` (the D-006 role table: train h_2..h_{n-1}, validate spike-free depolarising + h_1, report-only h_n and every rebound window), and `integrate_long_step(..., spec=, ih_protocol=, ls_window_mode=, n_drop_weakest=, n_drop_strongest=, dep_n_validation=, v_trough_min_mV=, train_all_hyp=)`. The old guards (12 mV cap, `n_long_train`) survive only as the `ih_protocol=False` path, which is what reproduces run B. |
| 2026-09-22 | 2-4 | **Gates.** `regression_passive_identity.py` still PASS after all three stages (loss max dL = 0 over 25 random theta; `gp_minimize` x and fun identical). `smoke_ih_fit.py` extended with S6 (ParamSpec is the passive spec), S7 (loader + roles on a synthetic archive cell with spiking depolarising sweeps) and S11 (six-parameter `fit_one_cell` end to end): **9/9 pass**. `synthetic_ground_truth.IhConfig` gains `vshift_mV` / `vshift_minf_mV` / `tau_scale` so ground truth can carry the two fitted kinetic knobs (Stage 7 needs it); defaults reproduce every pre-existing manifest. `mod/na.mod`, `mod/kv.mod` copied in so the generator can make spiking depolarising steps. |
| 2026-09-22 | 5 | **Phase 3 over the generic parameter vector.** `PARAM_NAMES` and the positional `loss_fn(x[0],x[1],x[2])` are gone from the bootstrap path: `_resolve_spec` / `_spec_names` / `_theta_of` / `_q_to_phys_cols` / `_other_axes_log_grid` / `_build_gp_profile` / `_validate_at_param_value` / `_refit_from_bundles` / `bootstrap_ci_for_cell` / `gp_diagnostic_for_cell` and the three plot helpers all key on the result's own `ParamSpec`; `BootstrapCIResult` gains `param_names`. Two things this fixes that a finite number would have hidden: (i) `_q_to_phys_cols` transforms **per axis**, so the LINEAR `dv_h` samples are no longer exponentiated into plausible, wrong CIs (S8 asserts dv_h stays inside [-10, 10] mV while gbar still equals exp(q)); (ii) `_run_one_parametric_iteration` / `_run_one_nonparametric_iteration` now pass `spec=_resolve_spec(opt_inputs)`, without which a 6-D `dims` met a 3-D refit and the worker's blanket `except` reported it as a skipped replicate. Profile likelihood in p > 3: a full inner meshgrid is 30^5 points, so p = 3 keeps the exact meshgrid and p > 3 uses a stratified Latin hypercube of the same budget M = inner^2 -- the profile is then an **upper bound** on the true one, stated in the docstring. |
| 2026-09-22 | 6 | **Orchestrator and submission.** `run_ih_fit.py`: `--arm {baseline_runB, passive_fullstep, ih6}` selects a complete configuration (axes, loader cap, role split, RMSD window, gate convention) that `--fit-params` and the per-axis `--*-bounds` can override; `--ih-mechanism / --ih-distribution / --ih-regions / --vshift-base / --ehcn` configure the mechanism; `--swc-dir` points at the diameter-corrected morphologies. It writes `phase2_results.csv` (legacy columns + the arm's own theta + `rail_<name>` + the derived I_h quantities, via `param_spec.flatten_result_params`), `ls_roles.csv` (D-006's audit trail) and `ls_diagnostics.csv` (per long step: window RMSD, the charge/sag/rebound sub-window RMSDs, and data-vs-model sag fraction / t63 / rebound, so the two deliberate exclusions of plan section 5 are checked rather than assumed). Phase 2.5 is OFF by default (D-006 Q8). **Two deliberate departures from `run_biological_fit.py`**, both loud: the two-pass auto-tau_w loop is dropped (`cm_profile_sweep.profile_cm` minimises over exactly two nuisance axes, so it is 3-D-only; run B used a single-point grid, so the scalar `--ss-tau-w-ms 5.0` reproduces it, and a comma grid is refused rather than collapsed), and the absolute-mV gate windows BOTH sides with the arm's own convention for the I_h arms (`gate_valid_via="same_window"`) instead of the legacy asymmetric early-window validation, which was designed to *minimise* I_h contamination. `submit_ih_fit.sh`: `qsub -v GROUP=...,ARM=...`, outputs under `<base>/<RUN_TAG>/<ARM>/<GROUP>`, `cd $CODE_DIR` before python (NEURON auto-loads `./x86_64` from the process working directory), an idempotent mtime-aware `nrnivmodl` guard behind an atomic `mkdir` lock (a group fan-out cannot race on one build), a post-build `hasattr(h, 'Ih')`/`'Ih_human'` verification, and `DRY_RUN=1`. |
| 2026-09-22 | 5-6 | **Gates, and one defect in the gates themselves.** `smoke_ih_fit.py` **11/11** (S8 generalised; S12 added: the arm table, the spec builder, and one end-to-end `run_ih_fit.main()` whose three CSVs are read back and asserted); `regression_passive_identity.py` still PASS bit-for-bit (max dL = 0 over 20 theta, x and fun identical). **Defect found and fixed in S6:** it built its cell BEFORE `make_ih_archive`, whose generator clears `h.allsec()`, so the cell's IClamp was orphaned and all 8 loss evaluations crashed -- and the assertion was `isfinite`, which the builders' `except -> 1e6` penalty satisfies. The check had been passing vacuously. S6 now builds the archive first and asserts the loss is BELOW the penalty and VARIES (range 0.085..0.493); S11, S12 and `run_ih_fit._assert_loss_live` were hardened the same way. Verified against the oracle folder that the crashes were an artefact of that smoke fixture, not of the pipeline: the oracle's own bootstrap on the same cell is clean, and so is a two-cell build-fit-destroy-build sequence (the production pattern). |
| 2026-09-22 | 7 | **Synthetic recovery, and the gate to Stage 8.** Three new modules and one entrypoint, each doing one thing. `synth_gt_grid.py` (copied from the synthetic benchmark and extended): the manifest now carries the two FITTED kinetic knobs as ground truth (`ih_dvh_mV` uniform on a LINEAR range, `ih_kappa_tau` log-uniform), the configuration shift that is not fitted (`ih_vshift_base_mV`), where the mechanism goes (`ih_regions`), and `is_fp_control`; plus `noise_sigma_from_results`, which reads the median `noise_sigma_mV` out of a completed run's `phase2_results.csv` so the cohort is generated as noisy as the cells the campaign will fit. The kinetic draws come from a FOURTH RNG stream, APPENDED not inserted -- `SeedSequence.spawn(4)` yields the same first three children `spawn(3)` did, so no pre-Stage-7 manifest moves (R1 asserts it against the untouched oracle folder, column by column). `gen_from_manifest.py`: `row_to_gt_kwargs` carries the knobs into `IhConfig`, tolerating a missing or NaN column by falling back to the published model; and `build_proto` now forwards `ls_dep_amplitudes_pA`, **without which every generated archive had an empty depolarising Long Square set and the D-006 validation set would have been empty**. `ih_recovery_report.py` (PURE: pandas and numpy, no NEURON, no fitting, no plotting in any statistic): the per-cell error table, the recovery summary, the C_m-inflation read-out, the false-positive table and the gate. `run_ih_recovery.py`: manifest -> generate -> fit -> report, where the fit is `run_ih_fit.main()` itself, so the gate is run on the program Stage 8 launches rather than on a copy of it. `submit_ih_recovery.sh` with the same `nrnivmodl` guard and a `DRY_RUN=1`. |
| 2026-09-22 | 7 | **Decisions taken in Stage 7** (user, 2026-09-22): the cohort is drawn on the REAL archive morphologies (`<ARCHIVE_ROOT>/<GROUP>/specimen_*/reconstruction.swc`), because identifiability of gbar and dv_h depends on how much membrane sits far enough out to sag; a quarter of the cells carry NO I_h as a **false-positive control** (gbar pinned at the box floor), which asks whether a 6-D fit rails gbar to the floor on a passive cell or invents an I_h and absorbs C_m into it; and the noise level is taken from run B's measured `noise_sigma_mV` rather than the benchmark default. Three measurement conventions follow, and are asserted: the error of a LOG axis is `log(theta_hat/theta)` and of the one LINEAR axis (`dv_h`) is a plain difference in mV (R4's fixture has a cell with `dv_h = 0` exactly, so a log ratio cannot produce the right answer); the I_h axes are summarised over the I_h cells only, because on a false-positive cell dv_h and kappa multiply a current that is not there; and railed axes are counted and reported, never silently dropped (`--drop-railed` is off by default). |
| 2026-09-22 | 7 | **Gates.** `smoke_ih_recovery.py` **9/9** -- R1 the appended RNG stream; R2 the manifest (ranges, the every-4th FP selection, the save/load round trip at 2.2e-13, the back-fill of a pre-Stage-7 manifest); R3 the generator plumbing; R4 the recovery arithmetic against a hand-computed fixture; R5 the C_m inflation and the FP table; R6 the gate in all five of its outcomes, including that a MISSING axis is a FAIL and not a silent pass; R7 the truth-vs-fit consistency check, item by item; R8 end to end (draw, generate, fit two arms, report); R9 byte safety. `smoke_ih_fit.py` still **11/11** and `regression_passive_identity.py` still PASS bit-for-bit. **Two defects found and fixed while writing the suite**, both of the same family as Stage 6's: (i) `run_ih_recovery` returned the in-memory manifest on the draw path and the re-read one on the `--manifest` path, so the two differed by an ulp and a 're-run of the same cohort' generated from a slightly different ground truth -- the file is now canonical in every path; (ii) the smoke's own `except Exception` did not catch `SystemExit`, which argparse raises, so a check that died took the suite with it and printed no FAIL line at all. |
| 2026-09-23 | 6-7 | **Paths derived, not hard-coded.** Both submit scripts had inherited `CODE_DIR="/davinci-1/home/ldellamea/Human Neurons Fitting/Ih Fit"` from the legacy passive-fit script, which is not where this code lives: the repo is cloned at `/davinci-1/home/ldellamea/TEEG/Towards-EEG` (laptop: `~/Towards-EEG`) and the folder is `<clone>/Passive Features/HPC script/Ih Fit` [user, 2026-09-23]. `CODE_DIR` is now `${PBS_O_WORKDIR:-$(pwd)}` -- submit from this folder -- with a refusal naming the exact `cd` when the entrypoint is not there. This also sidesteps `qsub -v`, whose list is comma-separated and cannot carry a path containing a space, which this one does. `ARCHIVE_ROOT` and `OUTPUT_BASE` become knobs, defaulting to the passive-fit working directory (the archives are NOT in git: there is no `specimen_*` anywhere in the repo). Every knob is prefixed `IH_` and a bare `CODE` / `ROOT` / `ARCHIVE_ROOT` / `OUTPUT_BASE` / `CODE_DIR` is reported and ignored, following the `H01_*` doctrine the paths reference adopted after a login-shell `CODE` sent a campaign into the wrong directory (sections 8.2, 8.4). Verified in three scenarios: submitted from the folder, submitted with a bare `CODE` exported, submitted from the wrong directory (exit 2 with the cd printed). Also withdrawn: the CRLF warning of the Stage-6 handover -- the repo's own `.gitattributes` already pins `*.sh`, `*.py` and `*.mod` to `eol=lf`, so a Windows clone cannot ship CRLF. |
