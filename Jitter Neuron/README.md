This pipeline shows how the jitter is applied to misalign the neurons away from the z axes.

The function that implements this logic is *set_rotations()* in the **Population** class.

See ***HybridLFPy Tweaked***
